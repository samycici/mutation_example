machine:
  ruby:
    version: 2.3.0
dependencies:
  override:
    - gem install bundler
    - gem install rake

test:
  override:
    - bundle install
    - rake test_rspec
    - rake mutant_rspec

general:
  artifacts:
    - "coverage/index.html"
